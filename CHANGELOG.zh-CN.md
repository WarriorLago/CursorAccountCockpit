# 更新日志

## [0.2.36] - 2026-08-17

### 修复

- 修复 0.2.35 安装包未打入前端资源导致启动报 `asset not found: index.html`。
- 更新清单解析兼容 UTF-8 BOM，避免 GitHub `latest.json` 触发 `UPDATE_MANIFEST_PARSE_FAILED`。

## [0.2.35] - 2026-08-17

### 修复

- 恢复导入无邮箱 Token：`cursor-token-lite/v1` 以及只有 `access_token` 的 JSON 可直接导入。
- 缺少邮箱时使用 `{auth_id}@cursor.imported` 占位，刷新额度后再回填真实邮箱。
- JSON 解析失败改为返回真实错误，不再一律显示「无法解析 JSON 内容」。

## [0.2.34] - 2026-08-16

### 修复

- 应用内更新改为下载并校验完整 NSIS 安装包；拒绝过小的引导程序 artifact，避免「只重启不升级」。
- 安装失败不再盲目拉起旧程序；成功后优先启动正式安装目录中的主程序。
- 自动更新失败增加短时熔断；支持跳过指定版本。
- 切号写入 `authId` / `cachedSignUpType`，无 refreshToken 时清除旧值。
- Cursor 运行中 TokenKeeper 不再热写 `state.vscdb`。
- 启动更新检查遵守外网开关与检查间隔。
- Windows 下账号详情加密密钥改为 DPAPI 包装；导出默认脱敏（完整备份需确认）。

## [0.2.33] - 2026-08-16

### 调整

- 正式版移除「截图演示数据」功能（仅调试截图用途，不应出现在发布包）。
- 设置页「自动下载并更新」改为独立分区与显式开关按钮，便于发现。

## [0.2.32] - 2026-08-16

### 调整

- 将「自动下载并更新」提升为设置页独立卡片，放在「应用更新」上方，避免嵌在更新卡片内不易发现。

## [0.2.31] - 2026-08-16

### 调整

- 启动时默认只检查新版本并提醒，不再从启动弹窗直接下载安装包。
- 设置页新增「自动下载并更新」开关（默认关闭）；仅开启后才会在启动时自动 OTA。
- 未开启自动更新时，提醒弹窗引导到设置页手动检查并安装。

## [0.2.30] - 2026-08-15

### 新增

- 设置页新增「截图演示数据」开关：用示例账号、账单与会话填充界面，便于对外截图；真实本机 Cursor 账号文件不会被改写，关闭后可恢复应用内缓存。
- 项目站点下载页展示 GitHub Releases 安装包累计下载次数，并按版本显示下载量。
- GitHub 个人主页与仓库 README 同步展示下载量徽章。

### 说明

- 本版本不内置应用内使用埋点；后续若做统计，将默认关闭并需用户显式开启。

## [0.2.29] - 2026-08-15

### 新增

- 新增 Warrior Ma 作者与支持弹窗，提供本地 GitHub 二维码、作者主页、项目 Star 和赞赏入口。
- 为 `WarriorLago/CursorAccountCockpit` 补充官方仓库、问题反馈、Sponsor 和项目支持元数据。

### 调整

- 作者弹窗聚焦展示 Warrior Ma 与 Cursor Account Cockpit，移除程序界面中的开源来源说明和跳转入口。
- 上游署名继续保留在公开源码文档与 `NOTICE.md`，不在产品界面内展示。
- 公开更新通道改为 GitHub Releases；仓库文档改为中英双语，并提供 Release 与项目站点入口。


## [0.2.26] - 2026-08-15

### 修复

- Cursor 账号额度的重置时间过长时继续使用省略号，鼠标悬浮或键盘聚焦后显示完整重置时间。
- 将提示层移到被省略文本的外层容器，避免提示内容随文本一起被裁切。

## [0.2.25] - 2026-08-14

### 优化

- Cursor 账号管理在常规桌面窗口中调整为每行 3 个账号，较窄窗口自动切换为两列或单列。
- 同时缩小账号卡片的宽高、内边距、间距、状态标签、配额区域、备注区域和操作按钮，让列表更紧凑。

## [0.2.24] - 2026-08-14

### 优化

- 设置页重新设计为紧凑卡片布局，窗口与托盘、账单导出并行数量、界面语言在桌面窗口中并排展示。
- 缩小设置卡片、图标、下拉框、按钮和间距，并降低阴影强度，减少大块留白。
- 增加三列、两列、单列响应式布局，在常规窗口和最小窗口宽度下均保持清晰。

## [0.2.23] - 2026-08-14

### 新增

- 设置页新增“窗口与托盘”，可选择点击关闭按钮时收进系统托盘或直接退出整个程序。

### 优化

- 点击主窗口最小化按钮时固定收进系统托盘，点击托盘图标即可恢复窗口。
- 新安装与旧版“每次询问”配置均默认按关闭到托盘处理，托盘菜单中的“退出”仍会完整退出程序。


## [0.2.22] - 2026-08-14

### 新增

- 新增“Cursor 会话管理”页面，只读读取本机 Agent 会话，展示主会话、子代理、项目、消息数、模型、更新时间和本地文件大小。
- 支持按会话标题、项目、会话 ID 和对话正文全文搜索，可显示子代理会话、选择当前列表或多选指定会话。
- 支持将已选会话合并导出为 Markdown，可选择是否包含工具调用详情，并可直接打开会话目录或本地 transcript 根目录。

### 优化

- 会话标题、项目路径、模型和状态优先从 Cursor 全局 SQLite 元数据补齐，对话正文从本地 JSONL transcript 流式读取。
- Cursor 会话数据库以只读方式打开；列表和导出均不改写 Cursor 的数据库、会话文件或账号凭据。

## [0.2.21] - 2026-08-14

### 修复

- 修复账单窗口右上角关闭按钮上半部分被 46px 标题栏拖拽层截获的问题。
- 补齐 Windows 根容器标识，并将完整 48×48px 按钮区域明确设为可点击的非拖拽区。

## [0.2.20] - 2026-08-14

### 修复

- 修复更新安装包下载超过 30 秒后在约 31%–40% 位置中断并提示 `UPDATE_DOWNLOAD_STREAM_FAILED` 的问题。

### 优化

- 更新下载总超时延长至 20 分钟，并强制使用原始响应，避免内容解码链路干扰安装包下载。
- 下载连接中断时最多自动重试 8 次，使用 HTTP Range 从已有进度断点续传，并在界面显示“网络波动，正在断点续传”。
- 为 v0.2.18/v0.2.19 的旧下载器发布小型更新引导程序，使旧版本也能在慢速网络下完成本次升级。

## [0.2.19] - 2026-08-14

### 新增

- 应用每次启动都会立即检查新版本，发现更新后直接弹出版本提示，支持“立即更新”和“稍后提醒”。
- 启动更新弹窗显示更新说明、安装包校验状态和实时下载进度，完成后自动安装并重新启动。

### 优化

- 更新界面隐藏具体更新节点地址，并收敛网络错误信息，避免向普通用户展示内部更新服务细节。
- 放大“Cursor 官方账单与收据”右上角关闭按钮至 48×48 像素，扩大点击热区，并允许导出过程中关闭窗口。
- 账单批量导出的默认并行数量由 3 调整为 5，设置页仍可在 1–10 之间选择。

## [0.2.18] - 2026-08-14

### 新增

- Cursor 官方账单与收据批量导出支持并行下载，默认同时处理 3 份，可在设置页调整为 1–10 个并行任务。
- 账单导出界面新增实时进度条，显示完成数量、总数、百分比、成功数量、失败数量、实际并行数量和最近完成项目。
- 新增应用内更新通道，启动后自动检查新版本，也可在设置页手动检查、查看版本与更新说明、下载安装。
- 安装包下载过程显示实时进度，并在安装前校验文件大小和 SHA-256；安装时自动退出、静默更新并重新启动。

### 优化

- 并行任务继续沿用单笔重试机制，并在全部任务结束后按账号生成逐笔明细、账号合计与全部账单总价统计。

## [0.2.17] - 2026-08-14

### 修复

- Cursor 自动检测新增运行进程、Program Files 系统安装目录和 PATH 命令入口，支持识别 `C:\Program Files\cursor\Cursor.exe`。
- 已保存的 Cursor 路径失效时自动重新检测并覆盖旧值，避免切号时报 `APP_PATH_NOT_FOUND:cursor`。

### 优化

- 将“Cursor 启动与切号”作为当前设置页首个配置卡片，直接展示检测状态、安装地址、保存、自动检测和手动选择按钮。

## [0.2.16] - 2026-08-14

### 新增

- 账单状态统计改为可多选按钮，点击已付款、无法收款、已作废、待付款、草稿即可决定列表显示和批量导出范围。

### 优化

- 默认仅选择已付款账单，已作废及其他状态只有主动选择后才会加入导出。
- 详细账单列表、预计 PDF 数量和账号数量会随状态选择实时更新。
- 批量导出按钮明确显示“仅已付款账单”或“包含已付款 + 无法收款的账单”等当前导出内容。

## [0.2.15] - 2026-08-14

### 新增

- 首次读取设置时自动检测并保存 Cursor 安装地址，已有地址时直接复用，不再重复扫描。

### 修复

- 切换 Cursor 账号时先完整关闭旧进程，再注入目标账号并自动重新启动 Cursor，确保新登录状态立即生效。

### 优化

- 设置页和快捷设置将路径字段明确标记为“Cursor 安装地址”，并说明自动检测与重启行为。

## [0.2.14] - 2026-08-13

### 优化

- 放大 Cursor 官方账单与收据弹窗，接近占满当前窗口。
- 缩小弹窗内控件与账单行的字号、间距，让常见桌面尺寸下一次约能看到 3 至 4 笔账单。

## [0.2.13] - 2026-08-13

### 修复

- 账单导出增加醒目的“选择全部月份”按钮，明确恢复跨月、跨年导出入口。
- 配额刷新优先采用 Cursor CLI 当前会话请求和 CLI User-Agent，并保留 Bearer Connect 接口作为回退。

### 优化

- 导出范围内分别显示已付款、无法收款、已作废、待处理、草稿及其他状态的账单数量。
- 勾选或取消“导出无法收款账单”后，直接显示本次实际纳入导出的账单笔数。

## [0.2.12] - 2026-08-13

### 新增

- Cursor 账号支持添加、修改和清空本地备注，并在卡片和列表中展示。

### 修复

- 配额刷新优先使用 `api2.cursor.sh` 的 Bearer Connect 接口，解决有效 Token 在旧 Cookie 配额接口返回 401 时刷新不出额度的问题，并保留 Cookie 接口作为回退。

### 优化

- 账单起止月份改为可滚动下拉框，月份较多时不再撑出屏幕。
- 导出页新增「是否导出无法收款账单」选项，默认不导出。
- `导出统计.txt` 改为按账号列出每一笔，再写账号合计，最后写全部账单总价。

## [0.2.11] - 2026-08-12

### 新增

- 官方账单 / 收据导出新增开始月份与结束月份选择，支持自定义跨月导出。
- 支持单月、两月、三月、更长跨度以及跨年区间。
- 导出文件夹与 TXT 统计文件按所选月份区间命名。

## [0.2.10] - 2026-08-12

### 优化

- 账号工具栏仅保留主入口「官方账单」「官方收据」按钮。
- 移除选择工具栏中重复的批量账单 / 收据按钮。
- 选中账号数量与批量导出行为仍通过主按钮保留。

## [0.2.9] - 2026-08-12

### 优化

- 账号列表适配可用窗口宽度，避免横向滚动。
- 压缩行内边距、操作按钮、配额间距与多余最小宽度。
- 同一行并排展示 Auto/Composer、API 与按量付费用量。
- 为缩短显示的账号、授权、套餐、标签、配额、重置与费用补充完整悬停文案。

## [0.2.8] - 2026-08-12

### 优化

- 调整账号卡片间距与配额区块层次，信息更清晰。
- 账号、授权、套餐、标签、配额、重置与费用的完整文案改由悬停提示展示。
- 配额标签允许自然换行，避免重要信息被省略号截断。
- 保持紧凑双列配额布局，并保留窄窗响应式表现。

## [0.2.7] - 2026-08-12

### 优化

- 缩小账号卡片内边距、间距、徽章与底部操作尺寸。
- 配额明细改为紧凑双列网格，同时保留全部用量项。
- 同一行卡片不再被最高账号强行拉齐高度。
- 窄窗口下继续使用单列响应式布局。

## [0.2.6] - 2026-08-12

### 优化

- 扩大 Cursor 官方账单弹窗的最大宽度与高度，提升大窗口下的空间利用率。
- 加宽并增高账号、月份下拉框和刷新按钮，提升选择操作体验。
- 新增“详细账单”区域标题与数量提示，大幅增加账单列表的可视高度并优化滚动条。
- 宽屏下将账单金额和操作按钮横向排列，同时补充窄屏与低高度窗口的响应式布局。

## [0.2.5] - 2026-08-12

### 新增

- Stripe 官方 PDF 遇到 TLS 握手、连接中断、超时及可重试状态码时，最多自动尝试 4 次。
- 批量导出改为自动创建 `cursor-年份-月份月账单` 文件夹，重复时追加全角序号，并将 PDF 直接平铺到文件夹。
- 导出目录新增 `导出统计.txt`，记录总数、成功数、失败数、逐项明细、每个账号金额及账单总价。
- 界面新增导出完成结果，明确显示成功与失败数量，结果保留至用户点击关闭。

## [0.2.4] - 2026-08-12

### 新增

- 新增 Cursor/Stripe 官方账单与收据 PDF 下载，支持单账号、已选账号和全部账号。
- 账单文件名固定为 `账号邮箱-YYYY-MM-DD.pdf`，导出时只选择目标文件夹。

### 修复

- 窗口右上角关闭按钮现在会直接结束桌面进程。

## [0.2.3] - 2026-08-11

### 修复

- 修复旧关闭确认弹窗已移除后，主窗口关闭按钮仍被拦截的问题。
- 点击主窗口关闭后会隐藏到托盘，结束程序使用托盘中的“退出”。

## [0.2.2] - 2026-08-11

### 调整

- 在账号主页新增运行时版本标识。
- 移除设置页的精简托盘说明卡片。

## [0.2.1] - 2026-08-11

### 修复

- 补充仅限 Cursor 账单 CSV 的 Tauri 文件写入权限，账单会直接保存到用户下载目录。
- 修复左侧导航选中项背景被覆盖的问题，提升选中文字对比度。

## [0.2.0] - 2026-08-11

### 调整

- 托盘精简为“显示主窗口”“设置”和“退出”。
- 界面默认使用简体中文。
- 新增精简设置页，可切换简体中文和英文。
- 从成品界面移除其他平台快捷入口、通用快捷设置、功能手册和应用多开页。
- 从桌面安装包移除未使用的其他平台菜单资源。

## [0.1.1] - 2026-08-11

### 修复

- 通过 Tauri 自定义协议嵌入生产前端，打包后的应用不再依赖 localhost。
- 将应用图标替换为新图标。

## [0.1.0] - 2026-08-11

### Cursor Account Cockpit by Warrior Ma

- 将桌面壳层重作为面向 Cursor 的账号与额度管理器。
- 支持按账号 / 月份下载 Cursor 用量账单。
- 支持分页拉取用量事件并生成 UTF-8 CSV 账单。
- 补充衍生项目署名与 CC BY-NC-SA 4.0 许可文件。
- 移除上游更新端点与构建期 CLIProxy sidecar 依赖。
